/**
 * Initial state from SearchForm component
 * @type {{origin: string, mobile: string, name: string, contractId: string, prospect_id_extern: string, dni: string, email: string, client_id: string, lastname: string}}
 */
export const initialState = {
  dni: '',
  phone: '',
  email: '',
  userId: '',
};
